# Dependencies
node_modules

# Build output (Vite uses build/ per vite.config)
build
dist

# Environment files (never commit secrets)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Logs and debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
*.swp
*.swo
Thumbs.db

# Optional: legacy Next.js (if any leftover)
.next

# Optional: test coverage
coverage

# Optional: Vite cache
.vite
